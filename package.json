{
  "name": "global-state-axios-hooks",
  "version": "1.0.1",
  "description": "`GlobalState` is a class for managing the global state of an application. `globalStateHooks` is a set of functions and React hooks that allow you to manage global state, handle promises, and make `axios` requests within React components.",
  "repository": "https://github.com/ghzcool/global-state-axios-hooks",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "/dist/*",
    "package.json"
  ],
  "scripts": {
    "build": "rimraf dist && tsc"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "axios": ">=1.6.1"
  },
  "author": "",
  "license": "ISC"
}
